var path = require('path');
var PATHS = require('./paths.js');

module.exports = {
  "@app": PATHS.APP,
  "@assets": PATHS.ASSETS,
  "@images": PATHS.IMAGES,
  "@videos": PATHS.VIDEOS,
  "@sass": PATHS.SASS,
};
